import { IconDefinition } from "@fortawesome/fontawesome-svg-core";
import { faBars } from "@fortawesome/free-solid-svg-icons/faBars";

/**
 * @packageDocumentation
 * Defines icon constants used throughout the application.
 *
 * This module imports and exports FontAwesome icon definitions to ensure
 * consistent icon usage across the application. It centralises icon management
 * and allows for easy updates to the application's icon set.
 *
 * @remarks
 * All icons are imported from FontAwesome libraries and exported as named constants
 * with descriptive names to make their usage intention clear in component code.
 */

export const menuIcon: IconDefinition = faBars;
