/**
 * This module serves as the entry point for the "Members" feature of the application.
 * It exports the {@link MembersPage} component, which is responsible for rendering
 * the main content of the "Members" page.
 *
 * @module
 */
import MembersPage from "./MembersPage";

export default MembersPage;
