import {ReactElement} from "react";
import {UnderConstructionSlider} from "../../components/UnderConstructionSlider";

export const UnderConstructionPage = (): ReactElement => {
    return (
        <UnderConstructionSlider/>
    )
}
