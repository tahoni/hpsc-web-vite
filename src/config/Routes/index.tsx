/**
 * @packageDocumentation
 * Barrel module for application routing exports.
 *
 * Re-exports the AppRoutes component which defines the React Router configuration
 * for the HPSC website. Keeping this as a barrel simplifies imports for consumers
 * within the app codebase.
 */
import AppRoutes from "./AppRoutes.tsx";

export { AppRoutes };
