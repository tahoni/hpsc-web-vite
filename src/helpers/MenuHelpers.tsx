import { PageMapping } from "../model/PageMapping.ts";
import { AboutUs, ContactUs, History, Home, Links, Members } from "./Items.tsx";

export const menuItems: PageMapping[] = [
  Home, Members, Links, History, ContactUs, AboutUs
];
