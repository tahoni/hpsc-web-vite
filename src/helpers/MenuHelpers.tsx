import { PageMapping } from "../model/PageMapping.ts";
import { AboutUs, History, Home, Links, Members } from "./Items.tsx";

export const menuItems: PageMapping[] = [
  Home, Members, Links, History, AboutUs
];
